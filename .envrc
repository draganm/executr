use flake

# Optional: Set project-specific environment variables
# export DATABASE_URL="postgresql://localhost/executr_dev"
# export APP_ENV="development"

# Optional: Create a local bin directory for project scripts
# PATH_add bin

# Optional: Load local .env file if it exists
# dotenv_if_exists .env.local